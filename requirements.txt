# Nova – Persönlicher KI-Assistent
# Abhängigkeiten

# -----------------------------------------------------------------------
# Kern (werden von Nova immer genutzt)
# -----------------------------------------------------------------------
# Nur Python-Standardbibliothek nötig – keine Pflichtabhängigkeiten!

# -----------------------------------------------------------------------
# Optional: Verschlüsselung (Kapitel 19)
# -----------------------------------------------------------------------
# Starke Fernet-Verschlüsselung (Fallback: XOR ohne dieses Paket)
cryptography>=41.0.0

# -----------------------------------------------------------------------
# Optional: Sprachausgabe / TTS (Kapitel 15)
# -----------------------------------------------------------------------
# pyttsx3>=2.90          # Plattform-TTS (Windows/macOS/Linux)

# -----------------------------------------------------------------------
# Optional: Spracheingabe / STT (Kapitel 15)
# -----------------------------------------------------------------------
# SpeechRecognition>=3.10.0
# PyAudio>=0.2.13        # Mikrofon-Support (benötigt PortAudio)

# -----------------------------------------------------------------------
# Optional: LLM-Backend (Kapitel 20)
# -----------------------------------------------------------------------
# openai>=1.0.0          # OpenAI-SDK (kompatibel mit anderen Endpunkten)

# -----------------------------------------------------------------------
# Optional: Erweiterte NLP-Funktionen (Kapitel 3)
# -----------------------------------------------------------------------
# spacy>=3.7.0
# de_core_news_sm @ https://github.com/explosion/spacy-models/releases/download/de_core_news_sm-3.7.0/de_core_news_sm-3.7.0.tar.gz

# -----------------------------------------------------------------------
# Tests
# -----------------------------------------------------------------------
pytest>=7.0.0
